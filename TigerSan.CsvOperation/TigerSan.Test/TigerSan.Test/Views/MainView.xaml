<windows:CustomWindow
    x:Class="TigerSan.Test.Views.MainView"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:windows="clr-namespace:TigerSan.UI.Windows;assembly=TigerSan.UI"
    xmlns:local="clr-namespace:TigerSan.Test"
    xmlns:pages="clr-namespace:TigerSan.Test.Pages"
    xmlns:vm="clr-namespace:TigerSan.Test.ViewModels"
    mc:Ignorable="d"
    Title="MainView"
    Width="1080"
    Height="720"
    WindowStartupLocation="CenterScreen"
    Style="{StaticResource CustomWindowStyle}"
    Background="{StaticResource Colors.PageBackground}">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <windows:CustomWindow.Resources>
        <vm:DesignMainViewModel x:Key="DesignData" />
    </windows:CustomWindow.Resources>

    <Grid>
        <TabControl
            Background="Transparent"
            BorderThickness="0"
            FontSize="16">
            <!--  列表：  -->
            <TabItem Header="列表">
                <pages:ListPage/>
            </TabItem>

            <!--  文本：  -->
            <TabItem Header="文本">
                <pages:TextPage/>
            </TabItem>
        </TabControl>
    </Grid>
</windows:CustomWindow>
